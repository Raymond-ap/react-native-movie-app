import { View, Text } from "react-native";
import React from "react";

const MovieSeries = () => {
  return (
    <View className="my-2">
      <Text className="px-4 capitalize text-white text-lg tracking-wider mb-2">episodes</Text>
    </View>
  );
};

export default MovieSeries;
